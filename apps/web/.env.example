# Vercel project env vars (Server-side only — no NEXT_PUBLIC_ prefixes here)
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Optional: room/identity prefixes used when minting join tokens
VOICE_ROOM_PREFIX=voice
VOICE_IDENTITY_PREFIX=user
